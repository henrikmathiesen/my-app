/* tslint:disable */

/*

getPlaceHolder(): SomeModel {
    const placeholder = new SomeModel();
    placeholder.name = 'Select something';
    placeholder.id = 0;

    return placeholder;
}

shiftPlaceHolder(some: SomeModel[]): SomeModel[] {
    const somr = [...some];
    somr.unshift(this.getPlaceHolder());

    return somr;
}

*/
